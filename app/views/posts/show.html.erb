<div class="header_admin">
  <h1>【管理者用】投稿詳細</h1>
</div>

<div class="main col-xs-12 col-md-8 offset-md-2">
  <div class="claim_table">
    <table class="table table-bordered">
      <tr>
        <th class="users_th text-center align-middle table-secondary">タイトル</th>
        <th class="users_th text-center align-middle"><%= @post.title%></th>
      </tr>
      <tr>
        <th class="users_th text-center align-middle table-secondary">投稿者</th>
        <th class="users_th text-center align-middle"><%= @post.user.contractor_name %></th>
      </tr>
      <tr>
        <th class="users_th text-center align-middle table-secondary">投稿日時</th>
        <th class="users_th text-center align-middle"><%= @post.created_at.to_s(:datetime_jp) %></th>
      </tr>
      <tr>
        <th class="users_th text-center align-middle table-secondary">投稿内容</th>
        <th class="users_th text-center align-middle"><%= @post.content %></th>
      </tr>
      <tr>
        <th class="users_th text-center align-middle table-secondary">画像</th>
        <th class="users_th text-center align-middle"><%= attachment_image_tag @post, :image, :fill, 300, 300, format: 'jpeg' %></th>
      </tr>
    </table>
  </div>
</div>
<div class="main_btn">
  <%= link_to "戻る", posts_path,class: "return_btn" %>
  <%= link_to "削除", "/posts/delete/confirm/#{@post.id}",class: "delete_btn" %>
</div>
</div>