<div class="header">
  <h1>お問い合わせフォーム</h1>
  <p>下記フォーマットにご入力いただき、「次へ」ボタンを押して内容をご確認のうえ、送信してください。</p>
</div>
<div class="main col-xs-12 col-md-8 offset-md-2">
  
    <div class="form">
    <%= form_with(model: @contact, local: true) do |f| %>
    <%= render 'layouts/error_messages', model: f.object %>
      <table class="table table-bordered table-hover table-md">
        <tr class="table-success">
          <th width="30%">お問い合わせの種類</th>
          <td>
            <%#= f.text_field :type, class: 'form-control' %>
          <%= f.radio_button :gender, :male, checked: true %>
          <%= f.label :gender, "男性", {value: :male, style: "display: inline-block;"} %>
          </td>
        </tr>
        <tr class="table-success">
          <th>件名</th>
          <td>
            <%= f.text_field :title, class: 'form-control' %>
          </td>
        </tr>
        <tr class="table-success">
          <th>電話番号</th>
          <td>
            <%= f.number_field :tel, class: 'form-control' %>
          </td>
        </tr>
        <tr class="table-success">
          <th>お問い合わせ内容</th>
          <td>
            <%= f.text_field :content, class: 'form-control' %>
          </td>
        </tr>
      </table>
    </div>
    
    <div class="main_btn">
      <%= f.submit 'お問いわせ内容を送信', class: 'next_btn' %>
      <% end %>
    </div>
</div>  

<div class="header_admin">
  <h1>お問い合わせ一覧</h1>
</div>
<% if @contacts.any? %>
  <% @contacts.each do |contact| %>
    <div class="claim_sub col-xs-12 col-md-8 offset-md-2">
      <div class="claim_table">
        <table class="table table-bordered">
          <tr class="table-primary">
            <th class="users_th text-center">お問い合わせ日</th>
            <th class="users_th text-center">お問い合わせ種類</th>
            <th class="users_th text-center">詳細へ</th>
          </tr>
          <tr>
            <th class="users_th text-center"><%= contact.created_at.to_s(:datetime_jp) %></th>
          　<th class="users_th text-center"><%= contact.type %></th>
          　<th class="users_th text-center"><%= link_to "詳細・返信確認へ",comment_path(contact.id) %></th>
          </tr>
        </table>
      </div>
    </div>
   <% end %>
<% else %>
  <p class="contract_nill">お問い合わせがありません。</p>
<% end %>
 
<div class="pagination  justify-content-center">
  <%= paginate @contacts %>
</div>

<div class="main_btn">
  <%= link_to '戻る',"/",class: "return_btn" %>
</div>